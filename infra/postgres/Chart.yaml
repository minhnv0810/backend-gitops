apiVersion: v2
name: postgres
description: Single Postgres instance shared by all services (database-per-service, one instance)
type: application
version: 0.1.0
dependencies:
  - name: postgresql
    version: ">=15.0.0 <16.0.0"
    repository: https://charts.bitnami.com/bitnami
