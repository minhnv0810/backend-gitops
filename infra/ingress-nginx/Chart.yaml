apiVersion: v2
name: ingress-nginx
description: Ingress controller, configured for local kind clusters (hostPort, no cloud LB)
type: application
version: 0.1.0
dependencies:
  - name: ingress-nginx
    version: ">=4.10.0 <5.0.0"
    repository: https://kubernetes.github.io/ingress-nginx
