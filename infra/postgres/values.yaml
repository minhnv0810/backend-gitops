namespace: backend

# Same image as backend/docker-compose.yml — no third-party chart, no
# dependency on any registry's tag-retention policy but Docker Hub's official image.
image: postgres:16-alpine

storage: 2Gi
